import connectToDB from "@/utils/database";
import User from "@/models/user";

export default async function Handler(req, res) {
  if (req.method === !POST) {
    return res.status(405).end().json({ message: "This is not a POST method" });
  }
}

try {
    if (isExisting){
        res.
    }
} catch (error) {
    
}